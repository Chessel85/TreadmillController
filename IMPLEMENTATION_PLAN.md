# Implementation Plan: Treadmill Controller

## How to use this file (read this first, every session)
1. Read the **Status** section below to see the current phase and next task.
2. Do **only** the next unchecked task — keep sessions small and focused so they fit comfortably in context.
3. Check the task off, update **Status**, and add a short dated note under **Session Log** if anything was learned, decided, or changed from the plan.
4. Commit the code changes and this file together.
5. If a task turns out to be bigger than expected, split it into sub-tasks in place rather than doing a huge session.

## Status
- **Current phase:** Phase 4 — Planned mode
- **Next task:** 4.3a rebuilt again (`protocol-tester.html`, Odysseus build) to log everything to a shareable `.txt` file instead of an on-page log. User needs to run it on the real treadmill, share the log to themselves, and paste it into chat so real command bytes can be identified; once known, 4.3b wires them into an actual auto-driving sequencer in `index.html`.
- **Last updated:** 2026-08-08

## Reference
Requirements: `Product Definition Document.md`. Full feasibility/risk analysis behind this plan's ordering is summarized in the Session Log below (2026-08-08 entry) rather than duplicated here.

---

## Phase 0 — De-risking spikes
Goal: answer the unknowns that could reshape scope, before investing in build-out. No production code required for these.

- [x] **0.1 BLE capability investigation.** Using a generic BLE scanner app (e.g. nRF Connect or LightBlue) on the iPhone, scan the JTX Sprint 6 and record: does it advertise BLE at all, what services/characteristics exist, is FTMS (`0x1826`) present, and — critically — is anything writable (not just readable)? Write findings to `docs/ble-findings.md`. **This determines whether Phase 4 (planned-mode auto-control) is buildable as scoped, or needs to be redefined as a read-only/cueing feature.** — Done, see `docs/ble-findings.md`. Result: writable control exists (proprietary `0xFFF2`), and standard FTMS is present for read/notify telemetry. Full auto-control remains in scope.
- [x] **0.2 Bluefy + VoiceOver spot check.** Confirm Bluefy's device-picker and permission dialogs are usable with VoiceOver on the target iPhone. Note any friction. — Done. User already runs a different BLE fitness-device page (`docs/reference/wahoo-kickr-example.html`, a Wahoo KICKR bike controller, hosted separately) successfully in Bluefy with VoiceOver: connect button → device picker → double-tap → connects, no friction reported. Confirms the connect-flow pattern works in the real target environment; no separate synthetic test page needed.
- [x] **0.3 Repo/deploy scaffolding.** Decide tech approach (plan: plain HTML/CSS/JS, no build step, to keep GitHub Pages deploy trivial — revisit only if a real need for a framework emerges). Enable GitHub Pages on the repo. Deploy a minimal static "hello world" page and confirm it loads on the iPhone via Bluefy. — Done. GitHub Pages enabled (main branch, root), live at https://chessel85.github.io/TreadmillController/. Placeholder `index.html` confirmed loading on-device via Bluefy. This placeholder will be replaced by the real page shell in 1.1.

## Phase 1 — Accessible shell & Bluetooth connect flow
Goal: the skeleton every other feature sits inside.

- [x] **1.1 Page shell & mode selection.** Semantic, VoiceOver-friendly landing screen: connect-to-treadmill entry point, then choice of Manual vs Planned mode (locked once a session starts, per PDD Ref #2).
- [x] **1.2 Treadmill connect flow.** `navigator.bluetooth.requestDevice` flow with an accessible "scanning / found / connected / failed" state sequence (PDD Ref #1). Verified on the real JTX Sprint 6 in Bluefy (Kraken build).

## Phase 2 — Manual mode logging (primary objective — build this before Strava/planned mode)
Goal: deliver the PDD's stated primary objective end-to-end, independent of whether Phase 0.1 turns out favorably.

- [x] **2.1 Session state machine.** Start / pause / resume / finish for manual sessions (PDD Ref #3, #6, #7). No treadmill control involved.
- [x] **2.2 Telemetry capture.** If Phase 0.1 found readable BLE characteristics, subscribe and log speed/incline changes with timestamps as they occur (PDD Ref #5). If not readable, fall back to duration-only logging.
- [x] **2.3 Local session storage.** Persist the in-progress and completed session log (e.g. `localStorage`/IndexedDB) so a dropped connection or accidental reload doesn't lose data.

## Phase 3 — Strava integration
Goal: get a finished manual session onto Strava — this alone satisfies the PDD's primary objective.

**Revised 2026-08-08** (see Session Log): Strava's API now requires an active paid Strava subscription ($11.99/mo, no free tier, applies to single-user personal apps too) for Standard-tier API access. OAuth auto-upload is dropped on cost grounds. Revised approach: generate a `.tcx` file client-side from the finished session and let the user upload it manually via Strava's free "Upload Activity" page — no API registration, no OAuth, no ongoing cost. Confirmed viable: user tested Strava's manual file-upload page in Firefox + NVDA and reported it's accessible.

- [x] **3.1 TCX generation.** Convert a completed manual session's `sessionLog` into a valid TCX file (absolute timestamps from session start + elapsed offsets; cumulative distance integrated from each segment's held speed × duration, per PDD Ref #5's "time and distance" minimum).
- [x] **3.2 Get the file off the phone.** A "Share / download session file" button shown after Finish: tries the Web Share API (`navigator.share` with a `File`) first, so iOS's native Share Sheet (Mail, AirDrop, Save to Files, etc.) opens directly with the file attached; falls back to a plain browser download if Bluefy doesn't support file sharing. User then uploads the file to Strava themselves (tested working with NVDA + Firefox on the user's PC).
- ~~**3.3 Re-auth/token refresh.**~~ No longer applicable — the revised approach uses no authentication at all.

## Phase 4 — Planned mode (contingent on Phase 0.1 findings)
Goal: PDD Ref #4 and #8. **Before starting, revisit Phase 0.1's findings and choose a direction:**
- If writable BLE control exists → build full auto-control as scoped.
- If only readable/no BLE control exists → redefine this phase as a "plan cueing" feature instead (app announces upcoming speed/incline changes via VoiceOver, user adjusts manually) and update this plan + the PDD to reflect the reduced scope.

- [x] **4.1 Plan file format + parser.** Format finalized from the user's draft at `rp1.txt`: full-line `#` comments; `incline:X% Speed:Ykmh Duration:Z` steps where incline is an integer 0–15, speed is a decimal to 1 place 0.0–20.0, and duration is `Nmin`, `Nsec`, or `NminMsec` (minutes 0–180, seconds 0–59, at least one part, minutes-then-seconds order only); `StartLoop:N` / `EndLoop` repeat blocks (nesting supported). Implemented as `parsePlanFile()` in `index.html` — all-or-nothing validation with line-numbered errors. A minimal file-input UI (not the full 4.2 picker) was added so it's testable on-device: choosing a `.txt` file in Planned mode parses it immediately and shows a loaded-plan summary or the error list.
- [x] **4.2 Plan upload/selection UI.** Upload a plan file from the user's PC; list previously used plans (PDD Ref #4). Implemented: `rememberPlan()` saves up to 5 most-recently-used plans (filename + text + summary) to `localStorage`; a "Previously used plans" list of one-tap load buttons appears above the upload field when any exist, capped at 5 to minimize VoiceOver scrolling. A stored plan that fails to reparse (e.g. after a future parser change) is dropped automatically rather than left as a dead entry. Confirmed working on-device — user noted the previously-used list isn't very valuable to them personally (iOS's file browser already opens to the last-used folder), so **keep this small/collapsible in 4.4** rather than expanding it further.
- [ ] **4.3 Drive or cue the session** per the direction chosen above. Split into sub-steps once real-hardware risk became clear (see Session Log):
  - [x] **4.3a Protocol discovery tooling.** The `0xFFF2` write byte format isn't confirmed for this specific console (open-source iConsole references are for other firmware variants/devices). Went through three iterations (see Session Log for full detail): (1) embedded fieldset in `index.html` — confusing/cluttering; (2) standalone `protocol-tester.html` with live service/characteristic enumeration (confirms whether a standard FTMS Control Point exists) and one spoken outcome per send; (3) **current ("Odysseus")** — replaced the on-page log with a full diagnostic-log `.txt` file (every discovered service/characteristic incl. read values, every send, every notify event, all timestamped) offered via a Share/download button identical in pattern to the main app's TCX export. User emails the log to themselves and pastes the contents into chat for direct analysis, since that's far richer than a spoken/typed summary. Uses its own version-naming scheme (Iliad/Odyssey names, not creatures — see the `feedback_protocol_tester_versioning` memory).
  - [ ] **4.3b Auto-driving sequencer.** Once 4.3a yields confirmed command bytes, build the real timer-driven walk through `currentPlan`'s steps in `index.html`, sending the right commands at the right times (still allowing manual adjustment mid-plan per PDD Ref #8).
- [ ] **4.4 UI/UX tidy-up pass.** Review button labeling/wording (e.g. the "Discard saved session" button) and improve the layout/grouping of buttons, labels, and counters across the page — several conditionally-shown elements have accumulated across Phases 1–3 without a cohesive visual pass. Also reconsider/shrink the 4.2 "previously used plans" list per the user's feedback above. (The 4.3a protocol tester no longer needs cleanup here — it's already a separate page, not part of the main app.)

## Phase 5 — Reliability & polish
- [ ] **5.1 Screen wake-lock + user messaging.** Request a wake lock during active sessions; clearly communicate (VoiceOver-friendly) that the phone must stay unlocked and Bluefy foregrounded for the session to keep logging (screen-lock kills the BLE session — confirmed during feasibility research).
- [ ] **5.2 Connection-loss recovery.** Handle a dropped BLE connection or accidental page reload mid-session without losing logged data.
- [ ] **5.3 Full VoiceOver pass.** Walk every screen end-to-end with VoiceOver on-device.
- [ ] **5.4 Real-session sanity check with Spotify.** Run a real session with Spotify streaming to Bluetooth headphones throughout, confirming no interference (expected to just work per feasibility research — this is a verification task, not a build task).

## Session Log
*(Condensed 2026-08-08 — see git history for the full blow-by-blow if needed.)*

- **Pre-plan feasibility review**: (a) JTX Sprint 6 BLE writable-control was unverified upfront — gated Phase 4 on 0.1; (b) iOS drops/throttles BLE on screen-lock/backgrounding — hence Phase 5.1; (c) Strava's client_secret couldn't be hidden in a static site — moot now, see the Phase 3 pivot below; (d) Bluefy is the only Web Bluetooth path on iOS; (e) simultaneous Spotify + treadmill BLE is sound — PDD Ref #10.
- **Phase 0 (complete)**: 0.1 — LightBlue scan found the treadmill (`iConsole+0168`) exposes proprietary `0xFFF0`/`0xFFF1`/`0xFFF2` (notify+write — the real control channel, undocumented iConsole protocol) and standard FTMS `0x1826`/`0x2ACD` (read/notify telemetry only, officially documented). Writable control exists, so Phase 4 auto-control stays in scope (will need the proprietary write format, not standard FTMS control). 0.2 — confirmed via an existing Wahoo KICKR Bluefy+VoiceOver page (`docs/reference/wahoo-kickr-example.html`) that the connect/device-picker flow works with no VoiceOver friction; also a strong pattern reference for later phases (plan-file format, session/timer UI). 0.3 — GitHub Pages live at https://chessel85.github.io/TreadmillController/.
- **Phase 1 (complete)**: Built the accessible shell (connect button, mode radios locked until connected) and the BLE connect flow. Real-device debugging found two issues, fixed together: Bluefy's Web Bluetooth polyfill needs full 128-bit UUID strings (not short hex like `0x1826`) in `optionalServices`, and rejects with non-standard error objects (bare error codes, not `DOMException`) — added `describeError()` to surface whatever Bluefy actually throws. Briefly swapped the `namePrefix` filter for `acceptAllDevices` to debug, then restored the filter once the real cause was fixed, since an unfiltered device picker is real VoiceOver-navigation friction (saved as a standing feedback memory — favor narrow/filtered lists everywhere, incl. future Phase 4.2). Established the mythical-creature version-naming convention this phase (saved to memory).
- **Phase 2 (complete)**: 2.1 — manual session state machine (Start/Pause/Resume/Finish), mode locked during a session per Ref #2. 2.2 — subscribes to FTMS `0x2ACD`, hand-decodes the flags-based payload, logs change-only speed/incline entries with elapsed-time timestamps; confirmed accurate against real console adjustments ("39 points recorded"). 2.3 — `saveSessionToStorage()` persists the session to `localStorage` on every change/tick/pause/finish, independent of in-memory UI teardown on disconnect, so a dropped connection doesn't lose data; a page-load check surfaces a recovered-session summary + discard button. Confirmed on-device: reload mid-session correctly recovered elapsed time and telemetry point count.
- **Phase 3 (core flow complete)**: Attempted OAuth registration but found Strava's API now requires an active paid subscription ($11.99/mo, no free tier, applies to personal single-user apps too) effective June 2026 — see the `project_strava_paywall_pivot` memory. Pivoted to no OAuth at all: `buildSessionTcx()` generates a minimal Garmin TCX v2 file from the session (absolute timestamps from `sessionStartTime` + elapsed offsets; distance integrated segment-by-segment from each held speed value, since `sessionLog` only records changes). A "Share / download session file" button (`shareOrDownloadSessionFile()`) tries the Web Share API with a `File` first — opens iOS's Share Sheet with the file attached — falling back to a plain download if unsupported. Confirmed on-device: Share Sheet opens, file attaches correctly to Mail, user uploads manually via Strava's own free "Upload Activity" page (also confirmed accessible with NVDA + Firefox). Added `title`/`text` to the share call so Mail could prefill subject/body — in practice Mail used the body (with the upload URL) but not the subject line; not a spec-guaranteed field, so that's expected/acceptable. PDD Ref #9 and this plan's Phase 3 section were updated to match the revised, auth-free approach.
- **TCX trackpoint frequency (2026-08-08)**: user asked about file size/limits for a 90–100 min training run. Confirmed there's no fixed sampling interval — points are added only on speed/incline change, so file size scales with how many adjustments a session has, not its duration; no practical size/count limit expected. Flagged that Phase 5.1 (screen wake-lock) isn't built yet, so a long run risks a mid-session BLE drop if the screen locks — 2.3's storage means data up to the drop wouldn't be lost, but the session would end early. User is training for a half marathon and doing 90–100 min sessions, so this is a real near-term consideration, not hypothetical.
- **Plan file format + parser (2026-08-08, Sphinx build)**: user finalized the `rp1.txt` spec (incline 0–15 integer, speed 0.0–20.0 one decimal place, duration as minutes and/or seconds with a 180 min / 59 sec cap, `StartLoop:N`/`EndLoop` blocks) and switched `.gitignore` to stop excluding `*.txt` so real plan files can be committed (pushing those separately themselves). Built `parsePlanFile()`: all-or-nothing validation, line-numbered errors, nested-loop expansion into a flat step list. Wired up a minimal file-input in Planned mode so a chosen `.txt` file is parsed immediately and shows a summary (step count + total duration) or the error list via the status region. Confirmed on-device: user loaded a plan file from their iCloud folder successfully. Not yet used to drive anything — that's 4.3.
- **Plan library (2026-08-08, Pegasus build)**: added a "Previously used plans" list in Planned mode — successfully-parsed plans are saved to `localStorage` (filename, raw text, step count, total duration), capped at the 5 most recently used, with one-tap reload buttons shown above the file upload field. Refactored the upload path and the library-reload path to share `applyPlanParseResult()` so summary/error handling isn't duplicated. Confirmed working on-device; user flagged it as low personal value (iOS's file picker already remembers the last folder) — noted for a shrink/simplify pass in 4.4 rather than removed outright now.
- **Protocol discovery tooling (2026-08-08, Minotaur build)**: before writing any real BLE control, researched the `0xFFF2` write protocol via WebSearch/WebFetch. Found only reverse-engineered protocols for *other* iConsole hardware — a bike-oriented Python project (`vmedea/iconsole`, `f0 a0`-prefixed commands with a sum-mod-256 checksum) and a blog post reverse-engineering a different iConsole *treadmill* (`f0 c3`-prefixed, only one command byte-string empirically confirmed, start/stop/speed/incline not decoded). Different byte-2 prefixes across sources confirm iConsole firmware varies by unit — nothing found is a safe, confirmed match for this JTX Sprint 6. Rather than guess real control bytes and ship them, asked the user how to proceed; they chose to test directly in the web app themselves (not standing on the treadmill while testing, so no personal risk). Built an "iConsole protocol tester" fieldset (shown once connected): raw hex → `0xFFF2` write button, live-scrolling log of `0xFFF1` notify payloads (capped at 20 lines), and three clearly-labeled unconfirmed preset byte strings (one from each source above) as quick-fill starting points. This is 4.3a; 4.3b (the real auto-driving sequencer) is blocked on the user discovering working command bytes with this tool.
- **Protocol tester rebuilt as a standalone page (2026-08-08, `protocol-tester.html`)**: user tried the embedded (Minotaur) tester — got "nothing received" on the one preset they tried — and gave two pieces of feedback: (1) the embedded fieldset was confusing/cluttering the main app, they wanted "a separate page dedicated to determining the correct way to control the unit"; (2) they recalled the JTX manual mentioning apps like Zwift being able to control the treadmill, which reads as evidence for *standard* control support and made the proprietary-only assumption feel unjustified. On (2): this doesn't necessarily contradict Phase 0.1's finding — apps like Zwift/QDomyos-Zwift are known to support many iConsole-based machines via device-specific proprietary implementations, not only standard FTMS — but it's worth re-verifying live rather than trusting the old scan, so the new tool re-enumerates services on every connect instead of assuming. Removed the embedded fieldset from `index.html` entirely (bumped to a new build, Hippogriff) and built `protocol-tester.html` as a standalone, unlinked page: enumerates all characteristics of each candidate service live via `getCharacteristics()` (would surface a Control Point if one exists), offers a target dropdown over *any* discovered writable characteristic, and — the main fix for "confusing" — pairs every send with exactly one spoken outcome (via `announce()`) rather than a hex log the user has to cross-reference manually. Asked the user for the exact manual wording to see if it names specific supported apps, which could narrow down which known protocol variant to try.
- **Manual quote received; protocol tester rebuilt to capture a shareable log (2026-08-08, Odysseus build)**: user pasted the JTX Sprint-8 manual's Zwift and Kinomap pairing steps. Both search for and select a device literally named "iConsole" — consistent with those apps having their own proprietary iConsole support (as expected), not evidence of standard FTMS control; noted in the `project_iconsole_protocol_unconfirmed` memory. User also asked for a different capture approach: rather than relying on spoken/typed summaries of what happened, write everything (full service/characteristic discovery including actual read values, every command sent, every notification received, all timestamped) to an in-memory log, and add a Share/download button — mirroring the main app's TCX export pattern exactly — producing a `.txt` file the user can email to themselves and paste into chat directly. Rewrote `protocol-tester.html` accordingly: `discoverAndSubscribe()` now calls `characteristic.readValue()` on every readable characteristic (not just listing its properties) and logs the hex/ASCII value; every discovery/connect/send/notify event appends a timestamped line via `logDiagnostic()`; `buildDiagnosticText()` + `shareOrDownloadDiagnosticLog()` package it all with a header (device name/id, generation time) into a downloadable/shareable text file. The on-page hex log and spoken per-send outcome were both dropped in favor of this — one clear "N entries recorded" counter plus the file is now the primary output. Established a new page-specific version-naming convention (Iliad/Odyssey names, e.g. this build is "Odysseus") distinct from `index.html`'s creature convention, per explicit user request — saved to memory.
- **Next**: user runs `protocol-tester.html` (Odysseus) against the real treadmill, shares the resulting `.txt` log to themselves, and pastes its contents into chat for analysis to find working start/stop/set-speed/set-incline byte sequences (or confirm/rule out standard FTMS control). Once known, implement 4.3b — the timer-driven sequencer in `index.html` that actually walks `currentPlan` and sends those commands at the right times. A UI/UX tidy-up pass (4.4) remains queued after that, covering the 4.2 plan-library shrink per user feedback.
